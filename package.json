{
  "name": "sai-wood-treats",
  "version": "1.0.0",
  "description": "SAI Wood Treats - Construction and Woodwork Portfolio",
  "type": "module",
  "scripts": {
    "dev": "node server.js",
    "build": "vite build && mkdir -p dist/assets && cp -r assets/vendor dist/assets/ && mkdir -p dist/assets/js && cp assets/js/main.js dist/assets/js/main.js && cp -r public/* dist/",
    "preview": "vite preview",
    "dev:vite": "vite",
    "pages:build": "npm run build",
    "pages:deploy": "npm run build"
  },
  "keywords": [
    "construction",
    "woodwork",
    "portfolio"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cloudinary": "^2.0.0",
    "cors": "^2.8.5",
    "dotenv": "^16.0.0",
    "express": "^4.18.0"
  },
  "devDependencies": {
    "@fullhuman/postcss-purgecss": "^8.0.0",
    "lighthouse": "^13.0.3",
    "terser": "^5.46.0",
    "vite": "^5.0.0",
    "vite-plugin-compression": "^0.5.1",
    "wrangler": "^4.65.0"
  }
}
